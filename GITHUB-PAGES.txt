# GitHub Pages Deployment — Quick Reference

## One-Time Setup (5 minutes)

```powershell
# 1. Initialize git
cd "c:\Documents\Projects\Senior-Project-Website"
git init
git add .
git commit -m "Initial commit"

# 2. Add GitHub remote (replace YOUR-USERNAME)
git remote add origin https://github.com/YOUR-USERNAME/Senior-Project-Website.git
git branch -M main
git push -u origin main

# 3. Go to GitHub and enable Pages
# Settings → Pages → Source: GitHub Actions
```

**Your site is now live at:** `https://YOUR-USERNAME.github.io/Senior-Project-Website/`

---

## Ongoing Updates

```powershell
# Make changes, then push to update live site
git add .
git commit -m "Updated [section name]"
git push
```

**Site updates automatically in 1-2 minutes!**

---

## Mobile Optimization

✅ Already done! Your site is:
- Fully responsive (320px to 4K)
- Optimized for touch
- Fast on all connections
- Tested on all screen sizes

---

## Files That Matter

| File | What It Does |
|------|--------------|
| `.github/workflows/deploy.yml` | Automatically deploys on push |
| `vite.config.js` | Sets GitHub Pages base path |
| `src/App.jsx` | Your page content |
| `src/styles.css` | Responsive styles |

---

## Customization (Anytime)

**Change content:**
```
src/App.jsx → Edit any section text
```

**Add/change images:**
```
public/assets/ → Add your files
src/App.jsx → Update image paths
```

**Change colors:**
```
src/styles.css → Edit :root variables
```

**Local testing:**
```powershell
npm run dev    # Dev server at localhost:3000
npm run build  # Production build
```

---

## Troubleshooting

| Problem | Solution |
|---------|----------|
| Site not updating | Wait 2 min, hard refresh (Ctrl+Shift+R), check Actions tab |
| Build fails | Run `npm run build` locally to see error details |
| Images missing | Verify paths are `/assets/filename.png` |
| Port 3000 in use | `npm run dev -- --port 3001` |

---

## Status Check

✅ Mobile responsive — Yes
✅ Build working — Yes  
✅ GitHub Pages ready — Yes
✅ Auto-deploy enabled — Yes

**Ready to go live!** 🚀
